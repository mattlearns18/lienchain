# Cowork Handoff — Phase 11 Pre-Investor Final Polish

**Status:** Ready to execute. One commit.
**Context:** Matt reported "invalid TX hash" on the XRPL explorer after settling a lien. Audit verified the underlying on-chain Payment IS real and `tesSUCCESS`, but the single-clinic settlement **success modal is broken UI** — it says "0 TXs on XRPL" and shows NO hash or explorer link. The hash only appears in the Liens tab. Plus the README.md is still Phase 1-era (Multi-Purpose Tokens, Hooks, RLUSD framing) and one hardcoded "testnet" string slipped through Phase 10. Three items in one commit, then engineering is truly investor-demo-ready.

## Part A — Single-clinic settle success modal shows TX hash + explorer link (CRITICAL)

**Repro:** Mint a single-clinic case via wizard (e.g. KC Pain $5K). Open Attorney View, select case. Mark Fiat Received. Click Settle Now → Execute Settlement. Wait for completion.

**Observed:** Final modal state reads:

```
Settlement Complete
PI-CASE-… — $5,000.00
✓ Verifying attorney credentials — Bar # on file
✓ All settlements confirmed — 0 TXs on XRPL
[ Done ]
```

No hash, no link. The user sees "0 TXs on XRPL" and has nothing to click.

**Reality:** `lien.tx2` in localStorage IS a real 64-char hash that resolves on XRPL testnet as a validated `tesSUCCESS` `Payment` transaction. The settlement WORKED — only the modal UI is dead.

**Expected behavior** (match the multi-clinic flow's success screen):

```
Settlement Complete
PI-CASE-… — $5,000.00
✓ Verifying attorney credentials
✓ Confirming lien details
✓ Executing settlement on XRPL
✓ Settlement complete — 1 TX on XRPL

KC PAIN & RECOVERY
[25FB364B…1AE314 ↗] ← click → opens testnet.xrpl.org/transactions/{full hash}

[ Done ]
```

The multi-clinic settlement flow's `AttorneyPreview.jsx` already implements this — it lists each clinic with name + truncated hash + explorer link. The single-clinic settlement is using an older code path that doesn't render the hash even though the data is there.

**Fix:** In `AttorneyPreview.jsx`, the success/completion section of the settlement modal needs to render the per-clinic hash + explorer link for BOTH single- and multi-clinic cases. The render that fires for multi-clinic should be the only render path; single-clinic is just a case where N=1. Refactor so single-clinic uses the same final-state render as multi-clinic — show the one clinic's name and its tx2 hash with the explorer link from `getNetworkConfig().explorer + tx2`.

The "0 TXs on XRPL" count bug is the same fix as Phase 10 Part G — count `payoutResults.filter(r => r.success).length` (or whatever the equivalent local variable is) and display that number. For single-clinic successful settlement, that's `1 TX on XRPL`.

## Part B — README.md wholesale rewrite

The README at the repo root (`/Users/matthewsabine/lienchain/README.md`) is still the Phase 1-era hackathon README. Investors clicking the GitHub link from the landing page or the README badge see this:

- "Tokenized PI medical receivables as **Multi-Purpose Tokens (MPT)**" — we use NFTokenMint
- "near-instant settlement via **RLUSD**" — testnet is native XRP, mainnet currency is TBD per MAINNET-READINESS.md
- "Programmable settlement via XRPL Hooks (WebAssembly)" — not used
- "**`issue-lien.js`** — Creates a PILIEN trust line" — CLI script, superseded by the React intake wizard
- "**`settle-lien.js`** — Accepts settlement amount and LienCo split %" — CLI script, superseded by the Attorney View settlement flow
- Planned features list includes "Clawback mechanism", "XRPL Hook for automated on-chain split enforcement", "React dashboard", "IPFS document anchoring", "Multi-lien batch issuance" — most are either built or defunct
- Status table lists "React dashboard: Planned" — shipped Phase 4; "RLUSD integration: In progress" — not in progress; "XRPL Hooks automation: Planned" — defunct
- Compliance table lists "MO / KS Lien Laws" — we support MO/TX/NV/IN, not KS; "NYDFS (RLUSD)" — not currently used
- Quick Start example uses `node settle-lien.js 8500 68` — the CLI scripts, not the production flow

**Rewrite the README to reflect Phases 3-10 actually shipped.** Suggested structure below — adjust copy to fit your voice, but the substance is what matters.

```markdown
# LienChain — Tokenized PI Medical Lien Settlement on XRPL

[![Live Site](https://img.shields.io/badge/Live%20Site-lienchain.vercel.app-blue?style=flat-square&logo=vercel)](https://lienchain.vercel.app)
[![Network](https://img.shields.io/badge/Network-XRPL%20Testnet-success?style=flat-square)](https://testnet.xrpl.org)

> Personal-injury medical lien financing on the XRP Ledger. Mainnet-ready behind a feature flag.

---

## Overview

LienChain is an operations and settlement platform for personal-injury (PI) medical lien financing. PI clinics treat patients on a lien — they're paid out of the eventual settlement, often 12-24 months later. That receivable is illiquid, hard to value, and impossible to trade. LienChain mints each lien as an NFToken on XRPL with structured metadata in the transaction memo (bill, split, clinic, market, attorney), enforces per-state compliance rules in the settlement waterfall, and executes real on-chain `Payment` transactions when the case settles.

- **Five US markets supported** — KC, STL, TX, NV, IN — each with state-specific compliance rules.
- **Multi-clinic case support** — a single PI case can group N clinics' liens, with shared reduction visibility.
- **Per-state lien priority** — Indiana 20% clinic floor enforced iteratively in the waterfall; Texas hospital lien priority surfaced as an attorney advisory.
- **Attorney provisioning** — operators send tokenized invite URLs to PI attorneys for case-specific portal access.
- **Real on-chain settlement** — each clinic payout is a verifiable XRPL `Payment` transaction with `tesSUCCESS` confirmation.
- **Mainnet-ready** — `VITE_NETWORK=testnet|mainnet` feature flag with separate seeds, loud network badge, and a pre-flight checklist (`MAINNET-READINESS.md`).

---

## How it works

1. **Lien tokenized.** Operator uses the intake wizard to mint a PI medical lien as an NFToken on XRPL. Multi-clinic cases supported — add additional clinics to an existing case, all sharing one attorney and one settlement waterfall.

2. **Case settles.** Attorney enters the gross settlement amount in the portal. The waterfall computes attorney fee, case costs, net available, and per-clinic pro-rata distribution with state-specific rules applied (Indiana floor, Texas advisory).

3. **Fiat receipt confirmed.** Operator records the attorney's wire or check arrival in LienCo's business bank account.

4. **On-chain clinic payouts.** Once fiat is confirmed, the platform issues one verifiable `Payment` transaction per clinic from LienCo's wallet to the clinic's destination address. Partial-failure recovery: any failed clinic is flagged for retry; idempotent re-runs only re-attempt failures.

5. **Audit trail.** Every transaction (mint and settlement) is permanently recorded on XRPL. Recovery rate, days-to-settle, exposure-by-market, and aging buckets surface in the operator dashboard.

---

## Tech Stack

| Layer | Technology |
|---|---|
| Ledger | [XRP Ledger](https://xrpl.org) (Testnet, Mainnet-ready) |
| Token Standard | NFToken (NFTokenMint) |
| Settlement Currency | Native XRP on testnet; mainnet currency TBD (stablecoin path per `MAINNET-READINESS.md`) |
| Frontend | React 19 + Vite + Tailwind + react-router-dom v7 |
| Charts | Recharts |
| Node SDK | [xrpl.js 4.6](https://github.com/XRPLF/xrpl.js) |
| Hosting | Vercel (auto-deploy on push to `main`) |

---

## Features Shipped

- **Lien Tokenization** — NFTokenMint with hex-encoded metadata, real testnet TXs
- **Multi-Clinic Cases with Shared Reductions** — N clinics per case, pro-rata distribution, case-level reduction request visibility
- **Per-State Compliance** — Indiana 20% clinic floor enforced in the waterfall, Texas hospital lien priority advisory banner, centralized `src/lib/markets.js` config
- **Portfolio Analytics Dashboard** — 6 KPI tiles (Total Deployed Capital, At-Risk Exposure, Recovery Rate, Avg Days to Settle, Active Cases, Settled Cases), Exposure-by-Market stacked bar chart, Recovery Rate Over Time line chart, Aging Buckets table
- **Attorney Provisioning** — operator-side registry, tokenized invite URLs, `/attorney/:caseId?token=…` validation gate, persistent attorney sessions
- **On-Chain Settlement** — real XRPL `Payment` transactions per clinic with `tesSUCCESS` enforcement, partial-failure recovery with per-clinic Retry, idempotent re-runs
- **Mainnet Readiness** — `VITE_NETWORK` feature flag, separate seeds per network, loud TESTNET/MAINNET badge, fiat receipt confirmation gate before settlement, `MAINNET-READINESS.md` pre-flight checklist

---

## Project Structure

```
lienchain/
├── README.md                  This file
├── CLAUDE.md                  Project brief for AI collaboration sessions
├── MAINNET-READINESS.md       Pre-flight checklist before mainnet flip
├── phase[3-10]-plan.md        Phase specifications
├── index.html                 Vite entry
├── vite.config.mjs
├── .env.example               VITE_NETWORK, seeds, WSS endpoints
│
├── setup-wallets.js           CLI: generates + funds testnet wallets
├── setup-markets.js           CLI: funds the 6-wallet panel
│
└── src/
    ├── main.jsx               Router: /, /dashboard, /attorney/:caseId, /attorney/demo
    ├── App.jsx                Landing page
    ├── Dashboard.jsx          Operator dashboard (5 tabs)
    ├── lib/
    │   ├── network.js         VITE_NETWORK config; seed + endpoint + explorer
    │   ├── markets.js         MARKETS / MARKET_INFO with per-state policy
    │   ├── waterfall.js       calcWaterfall — pro-rata + IN 20% floor enforcement
    │   ├── settle-onchain.js  executeSettlementPayment — real XRPL Payments
    │   ├── xrpl-tokenize.js   NFTokenMint via WebSocket
    │   └── xrpl-data.js       account_info, account_tx queries
    ├── components/
    │   ├── IntakeWizard.jsx
    │   ├── ReductionModal.jsx
    │   └── AttorneyPreview.jsx
    └── pages/
        └── AttorneyPortal.jsx
```

---

## Quick Start

```bash
git clone https://github.com/mattlearns18/lienchain.git
cd lienchain
npm install

# Configure environment
cp .env.example .env.local
# Set VITE_LIENCO_TESTNET_SEED (generate via setup-wallets.js or XRPL Testnet Faucet)

# (Optional) Generate testnet wallets for the 6-wallet panel
node setup-wallets.js

# Run locally
npm run dev          # http://localhost:5173
```

---

## Compliance

| Framework | Implementation |
|---|---|
| **HIPAA** | Patient data stays off-chain. Only structured lien metadata (bill, split, clinic, market) is hex-encoded into the NFTokenMint memo. |
| **State Lien Laws** | Per-state compliance rules in `src/lib/markets.js`. Indiana 20% clinic floor enforced in waterfall. Texas 72-hour filing window and hospital lien priority surfaced as advisory flags. |
| **UCC Article 9** | Lien metadata structured for UCC-9 assignment compliance. |
| **KYC / AML** | Wallet onboarding designed to integrate identity verification prior to mainnet deployment. |
| **Mainnet Readiness** | See `MAINNET-READINESS.md` for the pre-flight checklist (LLC formation, business bank account, E&O insurance, healthcare-lien attorney opinion letter). |

---

## License

MIT © LienChain
```

Adjust headers, badges, copy. The substance — current architecture, shipped features, current state structure, accurate compliance posture — is what investors need.

## Part C — Liens tab subtitle still hardcoded `testnet`

One straggler from Phase 10 Part C. The Liens tab subtitle reads:

> All liens issued on XRPL testnet — operator view

Should read from `getNetworkConfig().name` like the footer and dashboard subtitle do. After mainnet flip it should automatically read "All liens issued on XRPL mainnet — operator view" (or however you stylize the network name).

Fix is the same pattern as Phase 10 Part C — swap the hardcoded string for an interpolation:

```jsx
<p className="db-page-sub">All liens issued on XRPL {NETWORK_NAME} — operator view</p>
```

`grep -rn "[Tt]estnet" src/` after this fix should return zero hits outside `src/lib/network.js` and copy strings explicitly tied to the testnet network name in env example / fallback config.

## What NOT to touch

- The waterfall math, on-chain payment flow, attorney provisioning, network feature flag, fiat receipt logic
- `MAINNET-READINESS.md`
- `CLAUDE.md`
- `phase[3-10]-plan.md` documents
- App.jsx landing page (Phase 10 covered it)
- The CLI scripts at repo root (`setup-wallets.js`, `setup-markets.js`, `issue-lien.js`, `settle-lien.js`, `settle-real.js`)

## Acceptance criteria

1. **Single-clinic settle success modal** shows the real on-chain Payment hash for the settled clinic, with an explorer link that opens `testnet.xrpl.org/transactions/{hash}` (or livenet on mainnet). Reads "1 TX on XRPL" not "0 TXs on XRPL" at the final state.
2. **README.md** is rewritten with accurate Phase 3-10 framing — no MPT/Hooks/RLUSD-as-shipped references, no "React dashboard: Planned", current architecture and feature list, MO/TX/NV/IN compliance posture, current Quick Start.
3. **Liens tab subtitle** reads `All liens issued on XRPL ${NETWORK_NAME} — operator view` from the network config. `grep -rn "XRPL testnet" src/` returns zero hits.
4. **Multi-clinic settle success modal** still works exactly as today — don't regress the existing flow when fixing single-clinic.
5. Build clean, no console errors.

## Commit message

```
fix(phase11): single-clinic settle modal hash display + README rewrite + Liens subtitle

Three pre-investor-demo fixes:

  A. Single-clinic settle success modal now shows the on-chain Payment
     hash with an explorer link, matching the multi-clinic flow.
     Previously the final state read 'Settlement Complete · 0 TXs on
     XRPL' with no hash visible even though lien.tx2 was correctly
     written. Triggered Matt's 'invalid TX hash' report — the hash was
     fine, just never surfaced in the modal UI.
  B. README.md rewritten to reflect Phases 3-10 shipped — removes
     Phase 1-era MPT/Hooks/RLUSD framing, updates Tech Stack and
     Compliance tables, current architecture and feature list, current
     project structure. Investors clicking the GitHub link from the
     landing page now see a serious platform README.
  C. Liens tab subtitle now reads from getNetworkConfig().name instead
     of hardcoded 'testnet' (last Phase 10 Part C straggler).
```

## After committing

Push to `main` and append a status report to `.cowork-handoff-result.md` with:

- Commit hash
- Files modified (AttorneyPreview.jsx for single-clinic success modal, README.md for the rewrite, Dashboard.jsx or wherever the Liens tab subtitle lives)
- Confirmation each of the 5 acceptance criteria passes
- Any deviations
- "Ready for investor-demo final QA"

Cowork will run the final end-to-end check covering: fresh single-clinic mint → settle → verify the success modal shows the hash + clickable explorer link → click through to confirm the explorer resolves the TX as `tesSUCCESS`; rendering the GitHub link target to verify the README looks right; grep verification that "XRPL testnet" is gone from src/. After that clean, the platform is truly investor-demo-ready and the build is closed.
